# ssr-demo
Server-side rendering demo using Fastify and React

Once you clone this repository onto your machine run 'npm run build' then 'npm run start'. Navigate to `localhost:3000` in your favorite browser and you should see `SSR in React`!
